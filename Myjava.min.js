<script>
  $(document).ready(function () {
      var params = new URLSearchParams(window.location.search);
      var redirectUrl = params.get("redirect");

      if (redirectUrl) {
          console.log("Redirecting to:", redirectUrl);
          
          // Optional: Show a message like "Verifying..."
          $("body").append("<div style='position:fixed;top:0;left:0;width:100%;height:100%;background:#fff;text-align:center;padding-top:20%;font-size:18px;'>Verifying... Please wait</div>");

          setTimeout(function () {
              window.location.href = redirectUrl;
          }, 3000); // Wait 3 seconds before redirecting
      }
  });
</script>
